# action.yml
name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  report_file:
    description: 'Trivy Report file'
    required: true
    default: 'trivy.json'

runs:
  using: 'docker'
  image: 'Dockerfile'
  # args:
  #   - ${{ inputs.who-to-greet }}
